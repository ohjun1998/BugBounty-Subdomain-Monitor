name: BugBounty Subdomain Monitor

on:
  schedule:
    - cron: '0 */6 * * *' # 6시간마다 자동으로 실행
  workflow_dispatch: # 👈 이 줄이 반드시 있어야 'Run workflow' 버튼이 생깁니다!

jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: 'stable'

    - name: Install Tools
      run: |
        go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
        go install -v github.com/tomnomnom/anew@latest
        go install -v github.com/projectdiscovery/notify/cmd/notify@latest

    - name: Run Recon Pipeline
      env:
        NOTIFY_CONFIG: ${{ secrets.NOTIFY_SECRET }}
      run: |
        mkdir -p ~/.config/notify
        echo "$NOTIFY_CONFIG" > ~/.config/notify/provider-config.yaml
        
        if [ ! -f "targets.txt" ]; then
          echo "target.com" > targets.txt
        fi
        
        # 1. 수집된 신규 도메인들을 알림으로 바로 쏘지 않고, 'patch.txt'라는 임시 파일에 먼저 모읍니다.
        subfinder -dL targets.txt -silent | anew domains_old.txt > patch.txt
        
        # 2. 만약 신규 도메인이 존재한다면 (patch.txt 파일 크기가 0보다 크다면)
        if [ -s patch.txt ]; then
          echo "[+] 신규 서브도메인 발견! 알림을 전송합니다."
          
          # 임시 파일 내용을 통째로 읽어서 notify에 던집니다. 
          # 이렇게 하면 notify가 하나의 메시지(벌크)로 묶어서 디스코드로 전송합니다.
          cat patch.txt | notify -silent
        else
          echo "[-] 새로 발견된 서브도메인이 없습니다."
        fi
        
        # 3. 사용한 임시 파일 삭제
        rm -f patch.txt
        
    - name: Commit & Push changes
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add domains_old.txt
        git commit -m "Update domains_old.txt [skip ci]" || echo "No changes to commit"
        git push
