name: BugBounty Subdomain Monitor

on:
  schedule:
    # 💡 UTC 3시는 한국 시간 12시, UTC 9시는 한국 시간 18시입니다.
    - cron: '17 3,9 * * *'
  workflow_dispatch:

jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: 'stable'

    - name: Cache Go Binaries
      id: cache-go
      uses: actions/cache@v4
      with:
        path: ~/go/bin
        key: ${{ runner.os }}-go-bin-${{ hashFiles('.github/workflows/recon.yml') }}

    - name: Install Tools
      if: steps.cache-go.outputs.cache-hit != 'true'
      run: |
        go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
        go install -v github.com/tomnomnom/anew@latest
        go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
        go install -v github.com/projectdiscovery/katana/cmd/katana@latest
        go install -v github.com/tomnomnom/waybackurls@latest

    - name: Run Recon Pipeline
      env:
        DISCORD_URL: ${{ secrets.DISCORD_WEBHOOK }}
      run: |
        set +e
        
        if [ ! -f "targets.txt" ] || [ ! -s "targets.txt" ]; then
          echo "target.com" > targets.txt
        fi
        if [ ! -f "domains_old.txt" ]; then
          touch domains_old.txt
        fi

        touch raw_patch.txt all_passive_urls.txt filtered_urls.txt total_scan_list.txt live_results.txt

        # 1. 서브도메인 수집
        echo "[+] 서브도메인 및 패시브 URL 수집 중..."
        subfinder -dL targets.txt -silent | anew domains_old.txt > raw_patch.txt
        
        # 2. 분석 진입
        if [ -s raw_patch.txt ]; then
          while read -r root; do
            root=$(echo "$root" | xargs)
            [ -z "$root" ] && continue
            
            timeout 45s katana -u "$root" -passive -automatic-scoping -silent -field url >> all_passive_urls.txt 2>/dev/null &
            timeout 45s sh -c "echo '$root' | waybackurls" >> all_passive_urls.txt 2>/dev/null &
          done < targets.txt
          wait 

          if [ -s all_passive_urls.txt ]; then
            grep -Ei "\.(env|log|bak|sql|ini|git|conf|zip|tar|gz|yaml|yml|old|bkp|pwd)$|dashboard|admin|login|portal|config|swagger|api-docs|index-of" all_passive_urls.txt \
              | grep -Eiv "\.(png|jpg|jpeg|gif|css|js|woff|woff2|svg|ico|font)$" \
              | sort -u > filtered_urls.txt
          fi

          # 3. httpx 검증
          echo "[+] 수집된 모든 자산 실시간 응답 코드 확인 중 (Batch)..."
          cat raw_patch.txt > total_scan_list.txt
          if [ -s filtered_urls.txt ]; then
            cat filtered_urls.txt >> total_scan_list.txt
          fi
          sort -u total_scan_list.txt -o total_scan_list.txt

          if [ -s total_scan_list.txt ]; then
            cat total_scan_list.txt | httpx -sc -silent -t 200 -timeout 2 -retries 0 -no-color > live_results.txt
          fi

          # 4. 신규 리포트 조립
          REPORT_FILE="New_Subdomains_Report.txt"
          echo "========= 🚀 신규 자산 식별 리포트 =========" > $REPORT_FILE
          echo "스캔 일시: $(date)" >> $REPORT_FILE
          echo "============================================" >> $REPORT_FILE

          while read -r root; do
            root=$(echo "$root" | xargs)
            [ -z "$root" ] && continue
            
            touch temp_subs.txt
            grep -E "\.${root}$|^${root}$" raw_patch.txt | sort > temp_subs.txt
            
            if [ -s temp_subs.txt ]; then
              echo "" >> $REPORT_FILE
              echo "📌 [Root: $root]" >> $REPORT_FILE
              echo "--------------------------------------------" >> $REPORT_FILE
              
              while read -r sub; do
                sub=$(echo "$sub" | xargs)
                [ -z "$sub" ] && continue

                RES=$(grep -i "^http.*$sub" live_results.txt | head -n 1)
                if [ -z "$RES" ]; then
                  echo "🌐 $sub [No Response]" >> $REPORT_FILE
                else
                  echo "🌐 $RES" >> $REPORT_FILE
                fi
                
                if [ -s filtered_urls.txt ]; then
                  grep -i "$sub" filtered_urls.txt | sort -u | while read -r d_url; do
                    D_RES=$(grep -iF "$d_url" live_results.txt | head -n 1)
                    [ -n "$D_RES" ] && echo "   ↳ 🔍 Found: $D_RES" >> $REPORT_FILE
                  done
                fi
              done < temp_subs.txt
            fi
            rm -f temp_subs.txt
          done < targets.txt

          # 💡 [알아서 최적화 기능 추가]: 누적 히스토리 종합 파일 실시간 생성
          HISTORY_FILE="All_Subdomains_History.txt"
          echo "========= 🗂️ 누적 서브도메인 종합 리스트 =========" > $HISTORY_FILE
          echo "최종 동기화: $(date)" >> $HISTORY_FILE
          echo "총 자산 개수: $(wc -l < domains_old.txt | xargs)개" >> $HISTORY_FILE
          echo "==================================================" >> $HISTORY_FILE
          echo "" >> $HISTORY_FILE
          
          # 루트 도메인별로 이쁘게 분류해서 종합 파일에 저장
          while read -r root; do
            root=$(echo "$root" | xargs)
            [ -z "$root" ] && continue
            echo "📁 [Target: $root]" >> $HISTORY_FILE
            grep -E "\.${root}$|^${root}$" domains_old.txt | sort -u | sed 's/^/  ↳ /' >> $HISTORY_FILE
            echo "" >> $HISTORY_FILE
          done < targets.txt

          # 디스코드 파일 2개 동시 전송 (신규 리포트 + 종합 히스토리 파일)
          curl -H "Content-Type: multipart/form-data" \
               -F "content=🔥 **[Subdomain Monitor]** 스캔 완료! 신규 취약성 자산 리포트와 현재까지 모인 **종합 자산 히스토리 파일**을 함께 첨부합니다." \
               -F "file1=@./$REPORT_FILE" \
               -F "file2=@./$HISTORY_FILE" \
               "$DISCORD_URL" || true
               
          rm -f $HISTORY_FILE
        else
          # 신규 도메인이 없을 때도 종합 자산 리스트가 보고 싶다면 디코 알림에 총 개수 표현 최적화 적용
          echo "[-] 새로 발견된 서브도메인이 없습니다."
          TOTAL_COUNT=$(wc -l < domains_old.txt | xargs)
          curl -H "Content-Type: application/json" \
               -d "{\"content\": \"🔍 **[Subdomain Monitor]** 정기 스캔 완료. 새로운 서브도메인은 없습니다. (현재 누적 관리 중인 총 자산: **${TOTAL_COUNT}**개)\"}" \
               "$DISCORD_URL" || true
        fi

        rm -f raw_patch.txt all_passive_urls.txt filtered_urls.txt total_scan_list.txt live_results.txt temp_subs.txt New_Subdomains_Report.txt || true

    - name: Commit & Push
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add domains_old.txt
        git commit -m "Update domains [skip ci]" || echo "No changes to commit"
        git push || echo "Git push failed"
